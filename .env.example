# =============================================================================
# VIDA360 Environment Variables
# =============================================================================
# IMPORTANT: Never commit real values to version control!
# Copy this file to .env and fill in your values.
# =============================================================================

# Supabase Configuration (Required)
# Get these values from: Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Push Notifications (Optional - for real push notifications)
# Generate VAPID keys at: https://vapidkeys.com
# Or run: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key

# =============================================================================
# HOW TO CONFIGURE:
# 
# 1. Lovable: Go to Project Settings → Cloud → Secrets
# 2. Docker: Pass as --build-arg during docker build
# 3. Kubernetes: Configure in k8s/secret.yaml (base64 encoded)
# 
# VAPID Keys Setup:
# - VITE_VAPID_PUBLIC_KEY goes in .env (frontend)
# - VAPID_PUBLIC_KEY and VAPID_PRIVATE_KEY go in Supabase Secrets (backend)
# =============================================================================
